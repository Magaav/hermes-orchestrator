# Claim Registry

Use this file to prevent proofless upgrades. Status values are defined in
`README.md`.

| Claim | Status | Owner | Proof | Missing / Demotion Rule | Next |
| --- | --- | --- | --- | --- | --- |
| Configured production WASM Agent Native target is cloud-only. | verified | `/local` | `AGENTS.md`; `README.md`; `native/windows/release/win-unpacked/resources/native-defaults.json`; `native/android/release/WASM-Agent-arm64.native-defaults.json`; `plugins/wasm-agent/public/native/releases/latest.json`; config evidence only, not Windows package proof. | Any production doc that names local/dev origins as production is stale. | Keep guard in root and child native docs. |
| Production backend is `https://wa.colmeio.com`. | verified | `/local` | Same proof as cloud-only guard. | If a packaged release points elsewhere, demote affected platform claim. | None. |
| Production Windows app URL is `https://wa.colmeio.com/home?native=electron`. | verified | `native/windows` | Installed build `win-x64-20260820T175835Z` returned this exact route before and after full app restart with authenticated session. | Route proof does not imply Browser gesture proof. | Preserve the exact-route assertion in installed-session verification. |
| Localhost/dev origins are forbidden in production claims. | verified | `/local` | `AGENTS.md`; root README; native child contracts. | Tests/dev docs may mention local ports only as dev. | Keep docs scan active. |
| Self-improving harness contract exists for repeated inference. | verified | `docs/context` | `docs/context/HARNESS.md`; `docs/context/HARNESS_PROMISES.json`; `python3 tools/context/check-harness-promises.py` writes `reports/context/latest/harness-promises-result.json`. | If the registry validator fails or repeated manual loops are not harvested after the promotion threshold, demote to implemented-unverified. | Promote the next repeated expensive inference into `HARNESS_PROMISES.json` or record the missing primitive. |
| WASM Agent product-readiness evaluator contract exists. | verified | `docs/context` | `tools/context/evaluate-wasm-agent-product-readiness.py`; `docs/context/PRODUCT_READINESS_RESULT_SCHEMA.json`; registered promises `wasm-agent-product-readiness-contract-valid` and `wasm-agent-product-readiness-evaluation`; latest JSON/Markdown reports under `reports/context/latest/`. The evaluator defaults to artifact-only inspection; `--run <journey-id>` explicitly serializes that journey's promises. | This verifies the evaluator/normalization contract, not aggregate product readiness. Demote if schema validation fails, required metrics are hidden instead of `null`, stale evidence is promoted, or child execution becomes implicit. | Use the artifact-only report for status; run only the highest-priority stale/blocked journey with `--run`. |
| Current aggregate WASM Agent product readiness. | implemented-unverified | `plugins/wasm-agent`; `native/windows`; `native/android` | `reports/context/latest/wasm-agent-product-readiness-result.json`, run `readiness-20260820T145655Z`: `evaluationCompleted=true`, `ready=false`; repository and Electron/browser pass, while Android voice fails at `android_device_missing` (`missing-access`) with 11 required metrics missing. | Never treat evaluation completion, two passing lanes, or historical Android evidence as a full readiness pass. | Restore authorized Android ADB visibility through the installed Windows bridge and rerun only `--run android-voice-agent` against the preserved `20260820T145655Z` baseline. |
| Loop copilot checkpoint layer exists for active agent loops. | implemented-unverified | `docs/context` | `tools/context/watch-loop-copilot.py`; `docs/context/HARNESS.md`; `docs/context/VERIFY.md`; writes `reports/context/latest/loop-copilot-signals.json` and `.jsonl` when run. | It is not runtime proof and must not authorize success by itself; if it emits blockers, the named proof/demotion must happen first. | Run before slow native/runtime-control loops, rebuild-heavy work, or final claims. |
| Local Windows release metadata publishes build `win-x64-20260820T195241Z`. | verified | `plugins/wasm-agent`; `native/windows` | Feed and `VERIFY.json` match installer SHA `08b46bf491a25f7652a6765e2e7f17add00998cb488bccf8519880ba75ddbed1`; the feed guard passes. | Feed proof remains separate from installed behavior. | Keep the feed guard coupled to each Windows promotion. |
| Native evolution runtime/hot-op layer source/feed contract exists. | implemented-unverified | `native`; `plugins/wasm-agent`; `native/windows`; `native/android` | `native/NATIVE_EVOLUTION_CONTRACT.md`; `artifacts.runtime.launcher`; `artifacts.hotOps.android.hermesWakeProof`; `artifacts.hotOps.diagnostics.nativeDiagnosticsClassifier`; `node plugins/wasm-agent/tests/native_release_feed.test.js`; `cd native/windows/src && npm run test:windows-hot-ops`. | Source/feed tests do not prove an installed shell synced or activated the downloaded runtime. Keep unverified until installed Windows/Android diagnostics report active runtime/hot-op bundle IDs and SHAs. | Install/restart a shell with the native kernel, then run `python3 tools/windows/prove-hot-shell.py` and `python3 tools/doctor/wasm-agent-doctor.py`. |
| Final extracted Windows NSIS/app.asar package for `win-x64-20260820T195241Z`. | verified | `native/windows` | Final extraction produced app.asar SHA `9bda2adfbd2fbbcfc71be760ff0553e49884f71594840da01026127e82483dec` and exact source parity for full-power modules. The current installed raw-archive hash has not yet been re-read after this promotion. | Any package byte or verifier-contract change invalidates package proof; installed identity remains separate. | Re-run installed raw-app.asar projection on build `195241`. |
| Master:frontier full-power installed client authority. | verified | `plugins/wasm-agent`; `native/windows` | `reports/context/latest/master-frontier-full-power-client-runtime.json`: installed build `195241` advertised exact capabilities, arbitrary page-main-world JavaScript returned WhatsApp origin/readiness, and arbitrary PowerShell returned exit 0 as Windows user `Victor`. | This proves installed app-user authority, not Administrator/SYSTEM elevation or arbitrary authority from PWA/Android. | Keep harmless runtime canaries and exact capability selection in the promotion gate. |
| Windows login persistence fix status. | verified | `native/windows` | Full installed app restart retained exact Home route, `wa_uid`, durable expiration metadata, and authenticated `/auth/session` HTTP 200. | This does not prove a Browser page gesture. | Keep restart/session proof separate from Browser input proof. |
| Native Browser input receipt runtime. | implemented-incomplete | `native/windows`; `plugins/wasm-agent` | Installed build advertises receipt/pointer capabilities and receipt enable passes, but bounded synthetic dispatch failed closed with `surface_not_ready_for_pointer_dispatch`; surface status was loading/hidden. | No synthetic receipt, physical click, DOM target, or page action success is claimed. | Reconcile overlay visibility/readiness, then rerun dispatch plus on-demand inspection. |
| Android APK build `android-universal-20260626T160446Z` exists. | implemented-unverified | `native/android` | `native/android/release/release-manifest.json`; arm64/universal SHA `a65ceb358db42ce0b51db77fa6aa49568288ddb602ca5326caccf1257379dac9`; release feed labels it `not-runtime-verified`. | APK existence/feed publication is not signer, OAuth, voice, responsiveness, or installed runtime proof. | Run the behavior-specific package/runtime proof through the owning lane. |
| Current Android voice readiness journey. | implemented-unverified | `native/android`; `native/windows`; `plugins/wasm-agent` | Latest readiness result is `fail` at `android_device_missing` (`missing-access`), duration `76745ms`, 21.43% metric coverage, requested threshold `0.999`, and 11 missing required voice metrics. Production authority and hot-shell preflight passed on installed Windows build `win-x64-20260815T212625Z`, but no authorized Android device was visible to ADB through the Windows bridge. Older wake/runtime artifacts are historical or stale. | Cloud-local ADB emptiness is expected and cannot prove disconnection. Keep unverified until fresh Windows-bridge evidence covers positive/negative trials, exactly-once wake, false wakes, effective threshold, responsiveness, avatar/listening/transcription/routing/acknowledgement latency, and final receipt. | Restore Windows-bridge Android ADB visibility, then run `python3 tools/context/evaluate-wasm-agent-product-readiness.py --run android-voice-agent --compare reports/context/product-readiness/20260820T145655Z.json`. |
| Android OAuth native return fix status. | implemented-unverified | `native/android`; `plugins/wasm-agent` | Code/docs describe native return gating; no current connected-device OAuth report was inspected. | Browser/PWA callback success alone is not Android native success. | Run connected-device OAuth simulation and inspect `reports/sim/android/latest/result.json`. |
| Hermes Wake direct Android bridge shipping path. | implemented-unverified | `plugins/wasm-agent`; `native/android`; `tools/voice` | On 2026-06-16 the installed bridge uploaded `hermes-dataset-20260616T215020Z.zip`; import/verify passed with 50 positives and 82 negatives. `build/voice/hermes.onnx` was trained and is served by `https://wa.colmeio.com/native/android/hermes-wake-model/latest.json` with SHA `2abbebf21610f91f8d1fcfc12ac92f8ec19dc1191f3c90dbda4cba46e71027b2`; the Android bridge install request completed at `2026-06-16T21:57:35Z`. Native expected-model source constants were updated for a rebuild. The model exports a valid raw-PCM ONNX, but calibration failed the production acceptance gate. | Do not claim real wake-on-Hermes until installed-runtime diagnostics after service restart show ONNX available, voice wake enabled, WakeEngine ready, matching model SHA, inference count above zero, then runtime proof shows wake detection plus voice command dispatch. If using the new SHA as the hard acceptance model, rebuild/reinstall Android first. | Restart voice wake/app to load the installed model; if diagnostics need `model_sha_match=true` for the new SHA, rebuild Android, then run Hermes wake dry-run/debug classification. |
| Alexa wake production-candidate loop. | implemented-unverified | `plugins/wasm-agent`; `native/android`; `native/windows`; `tools/voice` | On 2026-06-20, Windows bridge proof passed and native-control/WAO evidence reached Xiaomi Mi 9 SE `db130d62` with active build `android-universal-20260620T115820Z`. Room-loop proof routed `alexa. open wake word` to `open_wake_word` with transcript `open wake word`. Confirmation-2 policy reached service diagnostics and a later `open settings` negative produced no timeline wake, but the confirmation-2 positive still emitted duplicate wake events. Direct ADB hot-op service start is now correctly classified as `service_start_rejected` because the Android wake service is non-exported. App-mediated `apply_wake_word_policy` cleared proof mode and applied Alexa, confirmation `2/700ms`, and cooldown `8000ms`, but installed service status still reports threshold `0.92` after requesting `0.999`. Fresh `app.responsiveness` events prove the Android WebView is locally overloaded with multi-second frame gaps, event-loop lag, and long tasks; `/health`/control latency is tracked separately as degradation. | Keep unverified until app responsiveness is healthy, threshold propagation is proven through the app-mediated policy path, avatar shine fires at wake/capture time with low latency, transcript capture no longer lingers after `open wake word`, fresh positive and negative proofs pass after the final live policy, and duplicate wake events stop. Hard-environment music/noise tests are phase two, not current baseline proof. | First fix or reduce app responsiveness overload and threshold propagation, then rerun Alexa positive/negative room-loop proof with responsiveness healthy, then continue avatar-shine and transcript-linger tuning before phase two. |
| App copilotability/live-introspection fast path. | implemented-unverified | `plugins/wasm-agent`; `native/android`; `docs/context` | Generic docs require agents to prefer runtime introspection/control, capability reports, diagnostics, UI/action summaries, and live policy knobs before guessing. Current Android implementation has source/static checks for native control command `get_runtime_snapshot`, capped visible-control summaries, capability acknowledgements, UX-budgeted polling/idle heavy work, public redacted Wake Word state, and live `apply_wake_word_policy` fields. Focused checks passed in this workspace: JS syntax, backend Python compile, Wake Word cloud tests, and static copilot sync checks. | Static/source checks do not prove an installed runtime is running the new control agent. Remote screenshots/log bundles must remain explicit heavy commands and must not run by default. | For future app work, use available runtime snapshot/state/capability/policy channels before asking the user to describe state or proposing rebuild/reinstall work. |
| Master:frontier V6 model-native repository and Electron UI lanes. | verified | `plugins/wasm-agent/server/master_frontier`; `plugins/wasm-agent` | Fresh readiness evidence: repository pass in `37713ms` with GPT-5.6 Sol, 7 provider calls, exact 26013 total tokens, and zero incorrect/unauthorized/human-intervention actions; Electron/browser pass in `51107ms` with production-selected GPT-5.6 Luna, 3 provider calls, exact 33431 total tokens, acknowledged client command and verified receipt, and zero incorrect/unauthorized/human-intervention actions. V6 is the browser default. Evidence paths are `reports/context/latest/master-frontier-v6-live-model-result.json`, `production-native-control-authority.json`, and `master-frontier-v6-client-ui.json`; kernel/auth/default/rollback proofs remain separately bounded. | Demote an affected lane when its declared invalidation paths change or its promise fails. These passes do not prove Android readiness, arbitrary MCP servers, universal repositories, Windows installer identity, or installed login persistence. | Rerun only the invalidated V6/readiness journey; use explicitly marked V5 selection for immediate rollback. |
| Frontier operator loop safety status. | implemented-unverified | `plugins/wasm-agent`; `native/windows` | Docs and code name fixed commands and auth gates. | Without focused route/control tests or live 403/200 proof, keep unverified. | Run focused server tests or gated curl proof. |
| Roadmap docs describe current software. | unknown | `docs/roadmap` | Roadmap docs are not runtime proof. | If a roadmap claim is not reconciled with product docs/code, classify as proposal/future/status only. | Reconcile when starting the track. |
