# Command Docs

`docs/commands` contains operator-facing command references and workflows.

## Current Docs

- `horc.md`: primary Hermes Orchestrator CLI reference.
- `discord-role-acl.md`: current Discord ACL behavior.
- `discord-acl-workflow.md`: operator workflow for Discord ACL changes.

Command docs must describe current command behavior. If scripts, slash commands,
or CLI flags change, update the matching command doc before closing the task.
