# Agents Public

`/local/agents/public` is the shared, repository-backed surface for reusable orchestrator assets.

Current canonical roots:

- `plugins/` -> shared plugin code for all orchestrator instances
- `scripts/` -> shared operational scripts for all orchestrator instances

`/local/plugins` and `/local/scripts` are compatibility symlink aliases that point here.
