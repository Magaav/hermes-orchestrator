# Retry-Window Challenge Evolution

`challenge_cases.py` is a compact curriculum for retry-window implementations.
Each case is `(limit, window_seconds, timestamps, expected_admissions)`.

Improve the cases so the registered mutation evaluator rejects all incorrect
implementations while accepting the reference implementation. Use at most six
cases and do not change the evaluator.
