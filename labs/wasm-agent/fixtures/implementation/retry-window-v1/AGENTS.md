# Retry Window Fixture

This fixture owns only `retry_window.py` and its focused tests. Preserve the
public `RetryWindow` API. Production fixes belong in `retry_window.py`; tests
are immutable benchmark evidence.
