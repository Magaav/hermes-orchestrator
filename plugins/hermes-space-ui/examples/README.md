# Hermes Space UI Examples

`examples/` contains sample payloads for the Hermes Space UI bridge.

## Current Files

- `dashboard_payload.json`: example dashboard/fleet payload.
- `node_action_payload.json`: example node action payload.

Keep examples aligned with the actual schemas and routes in `server/`. If a
route payload changes, update the matching example in the same change.
