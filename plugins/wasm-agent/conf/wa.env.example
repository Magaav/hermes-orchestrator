# Copy to wa.env and keep the real file out of git.
ADMIN_EMAIL=admin@example.com
USER_EMAILS=
GOOGLE_LOGIN_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com

# Production coordinates are non-secret. Cloud state must live outside this
# public plugin tree; explicit process environment values take precedence.
# HERMES_WASM_AGENT_DEPLOYMENT_MODE=cloud
# HERMES_WASM_AGENT_CLOUD_STATE_ROOT=/home/wasm-agent/.local/share/wasm-agent-cloud
# HERMES_WASM_AGENT_CLOUD_INSTANCE_ID=production
# HERMES_WASM_AGENT_PUBLIC_ORIGIN=https://wa.colmeio.com

# Optional: route Master:frontier envelopes directly to OpenAI Responses API.
WASM_AGENT_MASTER_FRONTIER_RECEIVER=openai-responses
WASM_AGENT_OPENAI_MODEL=gpt-5.5
WASM_AGENT_OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=

# Optional: use ChatGPT/Codex OAuth instead of the OpenAI API key lane.
# Tokens stay server-side. Prefer a Hermes/Codex auth JSON path over pasting a
# token; refresh is owned by the Hermes/Codex login flow.
# WASM_AGENT_MASTER_FRONTIER_RECEIVER=openai-codex
# WASM_AGENT_CODEX_MODEL=gpt-5.5
# WASM_AGENT_CODEX_BASE_URL=https://chatgpt.com/backend-api/codex
# WASM_AGENT_CODEX_AUTH_JSON=/home/ubuntu/.hermes/auth.json
# WASM_AGENT_CODEX_ACCESS_TOKEN=
